# Установка SonarQube на Ubuntu/Debian с Postgres Pro (для 1С)

Этот репозиторий содержит **автоматизированный скрипт** для установки и настройки [SonarQube Community Edition](https://www.sonarsource.com/products/sonarqube/) на системах на базе Debian/Ubuntu с использованием **PostgreSQL Pro 16 с патчами для 1С**.

Скрипт:
- Устанавливает Java 17
- Настраивает Postgres Pro 16 для 1С
- Создаёт БД и пользователя для SonarQube
- Скачивает указанную версию SonarQube
- Настраивает системные лимиты (`nofile`, `nproc`, `vm.max_map_count`)
- Создаёт отдельного пользователя `sonar`
- Настраивает `sonar.properties`
- Устанавливает **два systemd-сервиса**:
  - `sonarqube.service` — стандартный режим (production)
  - `sonarqube-console.service` — режим отладки (все логи в `journalctl`)

---

## 📥 Где взять версию SonarQube?

**SonarQube НЕ публикует версии в виде `25.3.0`!**  
Актуальные релизы — только на официальном зеркале:

👉 **https://binaries.sonarsources.com/?prefix=Distribution/sonarqube/**

Примеры корректных имён архивов:
- `sonarqube-9.9.8.100196.zip`
- `sonarqube-10.4.1.92868.zip`

❗ **Вы обязаны сами выбрать версию** и указать её в скрипте (параметр `SONARQUBE_VERSION` — только имя без `.zip`).

---

## ▶️ Как использовать

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш-логин/sonarqube-setup.git
   cd sonarqube-setup
   ```

2. Откройте `install-sonarqube.sh` и укажите нужную версию:
   ```bash
   SONARQUBE_VERSION="sonarqube-9.9.8.100196"  # ← сюда!
   ```

   При необходимости измените:
   - `DB_USER`, `DB_PASS`, `DB_NAME`
   - `INSTALL_DIR`, `SONAR_USER`

3. Запустите от root:
   ```bash
   chmod +x install-sonarqube.sh
   sudo ./install-sonarqube.sh
   ```

4. После установки:
   ```bash
   # Запуск в production-режиме
   sudo systemctl start sonarqube

   # Или в режиме отладки (логи в journalctl)
   sudo systemctl start sonarqube-console

   # Проверка статуса
   sudo systemctl status sonarqube
   sudo journalctl -u sonarqube -f
   ```

5. Откройте в браузере: `http://<ваш_сервер>:9000`  
   Логин/пароль по умолчанию: `admin` / `admin`

---

## ⚠️ Важно

- Скрипт **предназначен для чистой системы** (Ubuntu 20.04/22.04, Debian 11/12).
- Не запускайте `sonarqube` и `sonarqube-console` одновременно — будет конфликт.
- Для production **всегда используйте `sonarqube.service`**, а не `console`.
- Если вы **не используете 1С**, замените установку Postgres Pro на стандартный `postgresql`.

---

## 🛠 Автор
Михаил Губин, DevOps инженер
Собрано с учётом типичных ошибок в статьях (скачущие версии, ручные действия, опечатки).  


Можно сразу коммитить в репу!  
Нужен `LICENSE` или `.gitignore` — дайте знать.
